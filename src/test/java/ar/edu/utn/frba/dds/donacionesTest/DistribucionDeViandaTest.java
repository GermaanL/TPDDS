package ar.edu.utn.frba.dds.donacionesTest;

import ar.edu.utn.frba.dds.domain.colaboraciones.colaboracionesPersonaHumana.DistribucionDeVianda;
import ar.edu.utn.frba.dds.domain.heladeras.Heladera;

import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.Test;

import static org.mockito.Mockito.*;


public class DistribucionDeViandaTest {
 /*
    @Test
    public void testEnviarViandasADestino() {
        // Preparación de datos de prueba
        Heladera heladeraOrigenMock = mock(Heladera.class);;

        Heladera heladeraDestinoMock = mock(Heladera.class);
        // Preparación de datos de prueba
        when(heladeraOrigenMock.getCantidadDeViandas()).thenReturn(2); // Simulamos que la heladera de origen tiene 2 viandas
        // Creación de una DistribucionDeVianda
        DistribucionDeVianda distribucion = new DistribucionDeVianda(heladeraOrigenMock, heladeraDestinoMock, 1, "Prueba");

        // Ejecución del método a probar
        distribucion.realizarContribucion();

        // Verificación de que se llama a los métodos correctos en las heladeras
        verify(heladeraOrigenMock, times(1)).getCantidadDeViandas();
        verify(heladeraOrigenMock, times(1)).quitarVianda();
    }

  */
}